# Hyperliquid Premium Bot v3.1.0 — Pinned Dependencies
#
# Install: pip install -r requirements.txt
# Upgrade: pip install --upgrade -r requirements.txt
#
# All versions pinned for reproducibility and to avoid breaking changes.
# Tested on Python 3.11+ (Windows 10, Linux Ubuntu 22.04).

# ═════════════════════════════════════════════════════════════════════════════
# Core async & networking
# ═════════════════════════════════════════════════════════════════════════════
websockets==14.2          # WebSocket client (Hyperliquid WS)
aiohttp==3.14.3          # Async HTTP — CVE sweep: 66 advisories fixed (3.13.3-3.14.3)
aiohappyeyeballs==2.6.1  # Required by aiohttp (happy eyeballs algorithm)
aiosignal==1.4.0          # Required by aiohttp (>=1.4.0 for aiohttp 3.14)
propcache==0.5.2          # Required by aiohttp 3.14 (response payload caching)
requests==2.34.2          # Sync HTTP fallback — CVE sweep: .netrc leak + temp-file reuse fixed
urllib3==2.7.0           # Required by requests — CVE sweep: 12 advisories fixed (2.5.0-2.7.0)
charset-normalizer==3.4.2 # Required by requests / aiohttp
idna==3.18                # Required by requests / aiohttp — CVE sweep: crafted-input DoS fixed in 3.15
frozenlist==1.6.0         # Required by aiohttp
multidict==6.4.4          # Required by aiohttp
yarl==1.20.0              # Required by aiohttp
attrs==25.3.0             # Required by aiohttp / websockets

# ═════════════════════════════════════════════════════════════════════════════
# Web framework & real-time dashboard
# ═════════════════════════════════════════════════════════════════════════════
flask==3.1.3              # Web server (dashboard) — CVE-2025-47278 fixed in 3.1.1
flask-socketio==5.6.1     # Socket.IO for real-time dashboard — session fix for Flask >= 3.1.3
python-socketio==5.16.4  # Required by flask-socketio — CVE sweep: RCE + binary-attach DoS fixed
python-engineio==4.13.5  # Required by python-socketio (>=4.13.2)
bidict==0.23.1            # Required by python-socketio
werkzeug==3.1.8           # Required by flask — CVE-2025-66221 fixed in 3.1.4
jinja2==3.1.6             # Required by flask
markupsafe==3.0.2         # Required by jinja2
itsdangerous==2.2.0       # Required by flask
click==8.4.2              # Required by flask — PYSEC-2026-2132 fixed in 8.3.3
blinker==1.9.0            # Required by flask

# ═════════════════════════════════════════════════════════════════════════════
# Data & calculation
# ═════════════════════════════════════════════════════════════════════════════
pandas==2.2.3             # DataFrames (backtest, analysis)
numpy==2.2.5              # Numerical computing
scipy==1.15.3             # Research screening (Spearman / FDR helpers)
python-dateutil==2.9.0    # Required by pandas
pytz==2025.2              # Required by pandas
tzdata==2025.2            # Required by pandas (timezone data)
six==1.17.0               # Required by python-dateutil

# ═════════════════════════════════════════════════════════════════════════════
# Configuration
# ═════════════════════════════════════════════════════════════════════════════
pyyaml==6.0.2             # YAML config parsing

# ═════════════════════════════════════════════════════════════════════════════
# Hyperliquid live signing (testnet / mainnet)
# ═════════════════════════════════════════════════════════════════════════════
hyperliquid-python-sdk==0.23.0   # EIP-712 order signing
eth-account==0.13.7              # Wallet / private key handling

# ═════════════════════════════════════════════════════════════════════════════
# Security & encryption
# ═════════════════════════════════════════════════════════════════════════════
cryptography==50.0.0      # Encryption / signing — CVE sweep: 11 advisories (Bleichenbacher, subgroup) fixed
cffi==2.1.1               # Required by cryptography (>=2.0.0)
pycparser==3.0            # Required by cffi

# ═════════════════════════════════════════════════════════════════════════════
# Optional: Performance (Linux only)
# ═════════════════════════════════════════════════════════════════════════════
# uvloop==0.21.0          # Faster asyncio event loop (Linux/macOS only)
#                         # Uncomment on Linux for ~2x WS throughput
#                         # Windows: IGNORE (not supported)

# ═════════════════════════════════════════════════════════════════════════════
# Dev / Test (optional)
# ═════════════════════════════════════════════════════════════════════════════
pytest==9.1.1             # Unit test framework — CVE sweep: tmpdir handling fixed in 9.0.3
pytest-asyncio==1.4.0    # Async test support (pytest<10,>=8.4)

# ═════════════════════════════════════════════════════════════════════════════
# Optional: node_trades S3 rebuild (GoldRush parity fallback)
# ═════════════════════════════════════════════════════════════════════════════
# boto3==1.35.0           # Required only for real S3 downloads via
#                         # scripts/hl_node_trades_rebuild.py --execute
#                         # (requester-pays; AWS credentials required)
# lz4==4.3.3              # Optional: decompress LZ4-framed node_trades archives
